// Get these values from: Firebase Console → Project Settings → General
// → Your apps → Add app → Web (</>)  — use the SAME Firebase project as the
// Android app so both apps share the same live receipts data.
//
// Paste the config object Firebase gives you here, replacing the values
// below. Nothing will save until this is filled in correctly.

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
